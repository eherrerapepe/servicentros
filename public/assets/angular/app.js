//Instanciamos nuestro objeto de angular
var app = angular.module('servicentrosValvoline',['ngRoute']); //Le pasamos el ngRoute para que pueda cargar los links del menu

//Creamos el controlador principal de la aplicacion
app.controller('mainCtrl',['$scope','$http', function ($scope, $http) {
    
}]);